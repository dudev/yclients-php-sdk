/vendor/
/.phpunit.cache/
/.phpunit.result.cache
/megalinter-reports/
# This is a library, not an application — a lock file only pins versions for local dev here,
# and would otherwise mislead consumers into thinking it's authoritative for their own installs.
/composer.lock
